package $packageName;

import javax.annotation.Generated;
import org.opensaga.runtime.model.meta.*;
import org.opensaga.runtime.model.meta.Domain;

@Generated("$website")
public class $className implements Domain
{
#foreach($domainType in $domainTypes)
#set ( $className = $namingStrategy.getJavaClassNameForDomainTypeModel($domainType) )
#set ( $constantName = $namingStrategy.getJavaConstantNameForDomainTypeModel($domainType) )
 	public static final $className $constantName = new $className();
 	
#end

	public static class Relation {
	
#foreach($relationInfo in $relationTypes)
 		public static final RelationMetaModel $namingStrategy.getJavaConstantNameForMetaModel($relationInfo) = new RelationMetaModelImpl("$relationInfo.getId()", "$relationInfo.getName()");
 	
#end
	}
}